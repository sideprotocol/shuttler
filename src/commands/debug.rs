// use crate::{app::config, protocols::sign};

use super::Cli;

pub fn execute(_cli: &Cli, _txid: &String) {
    // config::update_app_home(&cli.home);
    // match sign::get_sign_task(txid) {
    //     Some(t) => {
    //         println!("Task variables \n{:?}", serde_json::to_string_pretty(&t).unwrap());
    //     },
    //     None => {
    //         println!("not found task: {}", txid);
    //     }
    // }
}